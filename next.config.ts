import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "efmqsnn2bjd.exactdn.com",
      "cdn.rareblocks.xyz",
      "images.pexels.com",
      "plus.unsplash.com",
      "images.unsplash.com",
      "example.com",
    ],
  },
};

export default nextConfig;
